name: weekly-research

on:
  schedule:
    - cron: "30 2 * * 1"   # every Monday 02:30 UTC
  workflow_dispatch: {}

permissions:
  contents: write

jobs:
  sweep:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - name: Run weekly sweep + audit + snapshot
        run: |
          python -m evergreen.cli weekly
          python -m evergreen.cli audit
          python -m evergreen.cli snapshot
      - name: Commit and push research updates
        run: |
          git config user.name "evergreen-bot"
          git config user.email "actions@users.noreply.github.com"
          git add data docs
          if git diff --cached --quiet; then
            echo "no changes"
          else
            git commit -m "chore(weekly): frontier AI research sweep $(date +%F)"
            git push
          fi
